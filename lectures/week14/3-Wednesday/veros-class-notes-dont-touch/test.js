// given the following code, console log the statement "hello world" only once, when the component mounts to the page. 

import React from 'react'

const Log = () => {
  return (
    <>

    </>
  )
}

export default Log

// map through data and render a compoent.  Pass a prop to this component 


// pass down a prop 


// get state from redux 

// dispatch an action 

//in this counter, setup a variable called count.  Increment this count variable when the button is clicked 


//chagne the following code to execute only once 
// what is the called
// change the code sod that it executes when

useEffect(() => {
  first

  return () => {
    second
  }
}, [third])




